ChachChat_All_Fixed + Online List (public signup preserved)

This zip is based on your working ChachChat_All_Fixed.zip.
It ONLY adds:
- Live Online list panel (right side)
- GET /api/online (auth required)
- SSE event: presence (auth required)

Public sign-up is unchanged.

Upload/overwrite all files from this zip, commit, and redeploy.
